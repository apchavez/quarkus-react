apiVersion: v1
kind: Service

metadata:
  name: product-api

spec:
  type: LoadBalancer

  selector:
    app: product-api

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080